# Deployed Google Apps Script Web App URL. It must end in /exec.
PUBLIC_APPLY_ENDPOINT=https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec

# Optional: dedicated Careers-only GTM container (must contain GA4 + PostHog only).
PUBLIC_GTM_ID=
