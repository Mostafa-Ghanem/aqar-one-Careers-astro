{
  "name": "aqar-one-careers",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npx tinacms dev -c \"npx astro dev\"",
    "dev:astro": "npx astro dev",
    "build": "npx tinacms build && npx astro build",
    "build:astro": "npx astro build",
    "preview": "npx astro preview",
    "check": "npx astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/sitemap": "^3.2.0",
    "astro": "^5.0.0",
    "tinacms": "^2.2.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@tinacms/cli": "^1.6.0"
  }
}
